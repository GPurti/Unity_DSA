using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ChangeSprite : MonoBehaviour
{
    /*private SpriteRenderer rend;
    private ChangeSprite fireBulletSprite, waterBulletSprite;

    // Start is called before the first frame update
    void Start()
    {
        rend = GetComponent<SpriteRenderer>();
        fireBulletSprite = Resources.Load<ChangeSprite>("FireBullet");
        waterBulletSprite = Resources.Load<ChangeSprite>("WaterBullet");
        rend.sprite = fireBulletSprite;
    }

    // Update is called once per frame
    void Update()
    {
        if(roomGameManager.elementsAvailable.lenght==1&& roomGameManager.elementsAvailable[0] == "Fire")
        {
            rend.sprite == fireBulletSprite;
        }
        if (roomGameManager.elementsAvailable.lenght == 1 && roomGameManager.elementsAvailable[0] == "Water")
        {
            rend.sprite == waterBulletSprite;
        }
    
    }*/
}
